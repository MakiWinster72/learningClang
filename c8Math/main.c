#include <stdio.h>
#include <math.h>

int main() {
    int x = 9;
    float y = 9.0;
    
    // x = sqrt(x); // 3
    // x = pow(x, 2); // 81
    // x = ceil(x); // 9
    // x = floor(x); // 9
    // y = round(y); // 9
    // x = abs(x); // 9
    // y = log(y); // 2.19722
    // y = sin(y); // 0.412118
    // y = cos(y); // 0.911130
    volume = (4/3) * PI * pow(radius, 3); // 计算体积
    printf("%d\n", x); // 3
    printf("Area: %lf", area); // 输出面积
    printf("\nSurface Area: %lf\n", suffaceArea); // 输出表面积
    printf("Volume: %lf\n", volume); // 输出体积